import 'package:flutter/material.dart';

class UnitsSuccessScreen extends StatelessWidget {
  const UnitsSuccessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
