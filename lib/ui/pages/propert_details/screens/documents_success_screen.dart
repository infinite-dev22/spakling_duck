import 'package:flutter/material.dart';

class DocumentsSuccessScreen extends StatelessWidget {
  const DocumentsSuccessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
